<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="20dp"
    android:gravity="center"
    android:background="@drawable/progress_dialog_bg_circle_corner_white">

    <ProgressBar
        android:id="@+id/progress_bar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:indeterminate="false"
        android:indeterminateTint="@color/ux_color_shadow_solid_line"
        android:paddingStart="0dp"
        android:paddingLeft="0dp"
        android:paddingEnd="20dp"
        android:paddingRight="20dp" />

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/progress_tip"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/rv_panel_annot_loading_start"
        android:textSize="20sp"
        android:textColor="@color/ux_color_black"
        android:maxLines="1"
        android:gravity="center"/>
</LinearLayout>
